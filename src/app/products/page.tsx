import Link from "next/link";
import {listPublicProducts} from "@/modules/commerce/data";
import {formatMoney} from "@/modules/commerce/domain";
export default async function ProductsPage(){const products=await listPublicProducts();return <main className="mx-auto max-w-5xl px-5 py-10"><h1 className="text-3xl font-semibold">商品</h1><p className="mt-3 text-[var(--text-secondary)]">The One Commerce foundation</p><div className="mt-8 grid gap-5 sm:grid-cols-2">{products.length===0?<p className="rounded-2xl bg-[var(--surface)] p-6">目前沒有可公開商品。</p>:products.map(p=><article className="rounded-2xl border border-[var(--border)] p-6" key={p.slug}><p className="text-sm text-[var(--text-secondary)]">{p.type} · {p.seller}</p><h2 className="mt-2 text-xl font-semibold">{p.name}</h2><p className="mt-3 text-sm">{p.shortDescription}</p><p className="mt-5 font-semibold">{formatMoney(p.price,p.currency)}</p><Link className="mt-5 inline-flex rounded-xl bg-[var(--brand-yellow)] px-4 py-2 font-semibold" href={`/products/${p.slug}`}>查看商品</Link></article>)}</div></main>}
